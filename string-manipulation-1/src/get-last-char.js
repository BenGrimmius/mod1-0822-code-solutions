/* exported getLastChar */

// The last index of the string needs to be selected.
// The last index of the string needs to be returned without the rest of the string.

function getLastChar(string) {
  var lastI = string[string.length - 1];
  return lastI;
}
