/* exported last */
// The last item in the array must be returned.
// use the length property - 1 to target last index
// The last index is returned.

function last(array) {
  return array[array.length - 1];
}
