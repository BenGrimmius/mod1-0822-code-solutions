/* exported head */
// The first item in the array must be returned.
// The function must target the array at its first index.
// The first index is returned.

function head(array) {
  return array[0];
}
