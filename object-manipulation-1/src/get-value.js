/* exported getValue */
// The key value for the object must be returned.
// In the code block, select the key value with bracket notation.
// return that value.

function getValue(object, key) {
  return object[key];
}
