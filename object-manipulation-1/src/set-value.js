/* exported setValue */
// The value of the object must be updated with this function.
// Assign the key parameter to the key parameter of the object parameter.
// No value is returned.

function setValue(object, key, value) {
  object[key] = value;
}
